# Moved

This document has moved to [`reference/docker.md`](reference/docker.md).

The guides/ folder is now organised in three layers: `platform/` (what the platform provides), `playbooks/` (end-to-end tasks), `reference/` (language and topic standards). See [`AGENTS.md`](../AGENTS.md#documentation-map) for the full map.

> Old direct links continue to work via this redirect, but please update bookmarks.
