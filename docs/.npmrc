# @entur/modal depends on @reach/dialog@0.16.2 which declares peer react@"^16.8.0 || 17.x".
# React 19 works fine at runtime but generates conflicting peer dependency warnings without this flag.
legacy-peer-deps=true
